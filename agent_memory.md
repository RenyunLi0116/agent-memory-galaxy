# Agent Memory Galaxy - Repository Memory

This file is both a lightweight project log and an example of the `agent_memory.md` format.

## doing / in progress
- [done] 2026-07-08 (claude/team-leader) Round 7 committed: (1)+(2) README.md + README.zh-CN.md overhauled to a cool, modern devtool README — centered header + tagline, honest shields.io badges (no fake star/CI/license), a wide clickable hero banner (media/hero.png → live Pages), a "Open the live demo" link, the 7 verified advantages as a feature list, side-by-side galaxy + team-console screenshots, 2-command quick start; new media/ holds 3 synthetic-only PNGs (strings-scanned, 0 private tokens). (3) opening animation reworked to the FLIP galaxy-into-demo-slot below. Final team-leader check: text+image leak scan 0, interactive galaxy-card==1, STEP 01-05, gates pass. Committed + pushed.
- [done] 2026-07-08 (claude/intro-anim) Round 7 — reworked the opening animation to a FLIP/shared-element "galaxy shrinks + flies into the demo card slot" (replaces round-6b's zoom-out-and-vanish). Generator-only (scripts/build-landing-concepts.py + regenerated docs/index.html + docs/assets/landing.css); did NOT touch README/media (other agent's scope) or landing.js. Verified precise landing (desktop delta dx/dy/dw = 0 via CDP real-time driver), all honesty fallbacks, bilingual 1920/390. NOT committed. See the round-7 entry below.
- [done] 2026-07-08 (claude/team-leader) Pre-commit privacy guard added to stop the recurring leak (subagents pasting the private-identifier denylist into public files). Committed token-free: scripts/precommit-privacy-check.sh (scans staged files) + scripts/install-privacy-hook.sh (installs .git/hooks/pre-commit). The actual patterns live in an untracked, gitignored `.amg-privacy-denylist` so the list itself never enters history. Enable per checkout: `./scripts/install-privacy-hook.sh`. Tested both ways (a file with a private token is blocked; a clean file passes). Public-repo only — not added to the private repo, which legitimately holds real data.
- [done] 2026-07-08 (claude/team-leader) Public git history scrubbed: rewrote the two commits whose agent_memory.md verification lines had quoted the leak-scan denylist regex verbatim (which itself listed real machine/IP/project identifiers). filter-branch tree-filter over the affected range replaced those literals with a neutral description; empty scrub commit pruned; force-pushed. Full-history scan now 0 hits; pages/demo/docs were never affected. Local backup branch kept for rollback (not pushed). Note: GitHub may retain the old commit objects reachable by direct SHA until its GC runs.
- [done] 2026-07-08 (claude/team-leader) Round 6b — cinematic 渐入渐出 intro rebuilt (FIXED) on the committed V2 base (f003a46). See the 2026-07-08 round6b entry below. Screenshots delivered for user review, NOT committed.

## 2026-07-08 - Round-7: FLIP intro — galaxy shrinks + flies into the demo slot (claude/intro-anim)
- What changed (generator-only, scripts/build-landing-concepts.py + regenerated docs/index.html + docs/assets/landing.css; landing.js untouched; README/media left to the other agent): replaced round-6b's "opacity→0 + scale→1.14 (zoom out and vanish)" exit with a shared-element FLIP where the demo galaxy fades in BIG & centered on the deep-space veil, holds, then SHRINKS + FLIES (JS-measured transform) precisely onto the live hero galaxy_card's slot and crossfades into the real interactive card. Mechanism: the veil's opaque background moved to `.intro-veil::before` (so it can dissolve without fading the flying galaxy above it). Page content is hidden by `html.intro-cine body>:not(.intro-veil){visibility:hidden}` (visibility, not display, so the head script can measure the live card's real rect while hidden) and revealed on `intro-flying`; the interactive `#after-galaxy` stays hidden until `intro-landed`, so exactly ONE galaxy is ever visible. The overlay galaxy is still the inert `galaxy_still` (data-cine-galaxy) → single-interactive-galaxy rule holds. Cinematic fade-in is `@keyframes cineShowGalaxy` with `animation-fill-mode: backwards` ONLY (must not hold forwards, or it would out-rank the inline FLIP transform). Head script (pre-paint, self-contained, safety-timed) orchestrates: `setTimeout(fly, SHOW_MS=1900)` → `applyFlip()` measures src(cine)+tgt(card) rects at fly time (layout final by then), sets `transform-origin:50% 50%` and `transform: translate(dx,dy) scale(tgt.w/src.w)` with a 1600ms `cubic-bezier(.45,.05,.2,1)` transition so src center→card center and src width→card width EXACTLY; `setTimeout(land, FLY_MS)` reveals the real card + fades the flown still (300ms crossfade masks any delta); `setTimeout(end, 4200)` hard safety net. Total show+fly+land ≈ 3.8s (slower/from-camera per user ask). Skip pill live from frame 1 via the same capturing delegated listener → `end()` jumps straight to settled. Responsive: rects measured at runtime, so desktop flies right into the right-column card, mobile flies DOWN ~200px into the card under the headline. Removed the old cineVeil/cineGalaxy/cineCap/cineHero keyframes and the `.hero-split` intro animation.
- Why: user feedback on round-6b — (a) galaxy "disappeared too fast", wanted slower/more cinematic; (b) did NOT want zoom-out-and-vanish, wanted the galaxy to slowly shrink and land in the exact spot/size of the hero's interactive demo card, then hand off to it. FLIP/shared-element is the clean way to get pixel-precise, responsive landing.
- Verification (kept honest): built a Node CDP real-time driver (chrome --remote-debugging-port + ws) because the headless virtual clock can't reliably capture mid-transition. Measured on the real timeline: DESKTOP lands EXACTLY (flyB delta dx=0 dy=0 dw=0 vs the card); MOBILE 390 lands centered & down (dw≈-2 at the captured near-end frame; targets the card rect so final is exact). Also verified: interactive galaxy_card exactly 1; overlay uses the inert still (data-mini-preview count = 1); ?intro=skip / prefers-reduced-motion / no-JS all render the settled hero with veil.display=none, card.visibility=visible, no overlay; sessionStorage `amg_intro_cine` still one-play-per-session; STEP 01-05 continuous; en copy has no " - " (the 3 hyphens in index.html are JS subtraction in the head script, not copy); zh has no 「合成」; no "MIT"; private-identifier grep 0 across docs; console has 0 script errors/exceptions (only a benign /favicon.ico 404 present on every page incl. no-JS); concepts gallery regenerates & renders; bilingual 1920 + 390 no 破版. Screenshots delivered for user review, NOT committed.
- Note: dropped the round-6b `?introcap=show|fly|flyend` freeze hooks I first prototyped — they measured the card too early (before layout settled → wrong landing) and are redundant now that the CDP real-time driver captures clean frames. Capture params kept: `?intro=play` (force) / `?intro=skip` (settled).

## 2026-07-08 - Round-6b: fixed cinematic 渐入渐出 intro overlay (claude/team-leader)
- What changed (generator-only, scripts/build-landing-concepts.py + regenerated docs): added a one-time cinematic opening on top of the V2 hero, engineered around every V1-splash rejection. Mechanism: a fixed full-screen deep-space veil (`.intro-veil`, NOT `.intro-cine` — the html gate class is `intro-cine`, so a bare `.intro-cine{display:none}` would blank the whole page; this collision was the first bug found and fixed) holds an inert `galaxy_still` (no canvas / no data hooks / no role=button → single interactive galaxy preserved) center-stage. Timeline is 100% CSS @keyframes (2.3s) gated by `html.intro-cine`: cineGalaxy fades the demo galaxy in (opacity 0→1, scale .82→1) → holds (~0.6s, scale 1.035) → dissolves (opacity→0, scale→1.14); cineVeil keeps the veil opaque to 66% then fades it out, ending `visibility:hidden;pointer-events:none` (fill:forwards) so content is never blocked even with zero JS teardown; cineHero (one container animation, no per-child stagger) rises the settled hero in during the veil fade for a cross-dissolve "galaxy lands into the hero card" handoff. A high-contrast gold Skip pill (`.intro-skip`, gold border + #ffdd94 text + dot) is rendered from frame 1; the head script wires it via a capturing delegated document click listener so it works before landing.js parses (fixes V1's dead Skip). Head script (pre-paint, in <head>, self-contained): gates on `prefers-reduced-motion` + sessionStorage `amg_intro_cine` + `?intro=play`/`?intro=skip`; adds `intro-cine`, then `setTimeout(end,2350)` primary + `setTimeout(end,2600)` hard safety net (both in <head>, fire even if landing.js never loads). Base (no class) = overlay display:none + settled hero, so no-JS / reduced-motion / repeat visits / ?intro=skip get the finished V2 hero with zero motion. reduced-motion @media guard force-hides the veil + forces hero opacity 1 (also neutralises the global `*{animation:none}` rule that would otherwise freeze the veil opaque). New i18n key `cineTag` en "One galaxy. Every agent." / zh "一张银河，容纳每个 agent。" (paired) + a non-translated brand kicker "AGENT MEMORY GALAXY". Overlay markup is last in <body> (backward SVG-filter-def reference like the contrast still).
- Why: user wants the electric 渐入渐出 opening back, but fixing the round-6 V1 splash's blockers: (1) V1 was a text card over pure black — now the DEMO GALAXY is the visible subject; (2) V1's Skip never rendered — now visible+clickable from frame 1; (3) short ≤2.5s + JS safety timer; (4) once-per-session; (5) reduced-motion/no-JS static; (6) settled state stays the V2 hero with the demo in the first viewport; (7) single interactive galaxy (overlay uses the inert still).
- Status: done. NOT committed (awaiting user approval).
- Verification: python ast parse OK; `python3 scripts/build-landing-concepts.py` clean; `node --check landing.js` OK. Grep gates on regenerated docs/index.html — interactive galaxy-card==1, canvas==1, role=button==1, intro-veil overlay==1, single Skip button, STEP/步骤 01-05 continuous, cineTag paired en+zh, MIT 0, 合成 0, en " - " 0, fictional 3 / synthetic 0 (≤3), leak scan against the private-identifier denylist 0, overlay labels use only PRESENCE LAYER wording (no misleading live/online text; the 2 "live" hits are the mini-edge tone CSS class, same as the committed contrast still). Deterministic keyframe capture via temporary baked animation-play-state:paused + negative animation-delay in the built CSS (removed by rebuild afterwards) + real-time one-shot `google-chrome --headless --screenshot` (virtual-time-budget was avoided — it dropped the animating caption from the paint). CDP (single instance) confirmed: console 0 page errors on landing en/zh + intro-play + concepts + reduced (favicon 404 ignored); scrollWidth==innerWidth at 1920 and 390 (no h-overflow); reduced-motion (Emulation.setEmulatedMedia + the --force-prefers-reduced-motion flag) → introcine=false, overlay display:none, canvas off, static SVG shown; once-per-session → load1 plays (intro-cine present), load2 does NOT (settled); ?intro=skip → no overlay. Frames Read-checked (渐入→hold→渐出→settled, en+zh, skip-closeup, reduced-motion, mobile 390 intro+settled, full-page 1920 en+zh, concepts gallery): galaxy is the visible subject, gold Skip pill present early, bilingual tagline renders (zh full-width punctuation, no orphans), settled hero shows the demo galaxy in the first viewport on desktop+mobile, concepts unaffected. IMPORTANT env note for future agents: `pkill -f "google-chrome"` (and pkill on the devtools port) aborts the sandboxed Bash call with no output — kill chrome by explicit PID or just use fresh per-run ports/profiles instead. Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/round6b/shots/.
- Files: scripts/build-landing-concepts.py (generator); regenerated docs/index.html, docs/assets/landing.css, docs/assets/landing.js (docs/concepts/* re-emitted byte-identical — intro is landing-only).
- [done] 2026-07-07 (claude/team-leader) Promo round 6 — demo forward + intro animation DONE (committed with round-5). 2 competing variants (V1 cinematic full-screen splash / V2 in-place demo fade-in reveal) → team-leader first picked V1-base + V2's fuller hero card → but the 2 reviewers (conversion 6.5, mobile-zh 7) flagged the V1 splash as blocking the demo for ~3s (contradicting goal ② see-demo-immediately), having a Skip-render bug, and not actually animating the demo galaxy → fix pass PIVOTED to V2's in-place reveal (demo galaxy fades in on load, Skip pill always visible, once-per-session, reduced-motion static) and fixed V2's only high (renamed the fictional demo card's "ONLINE HUD / live agents" → "PRESENCE LAYER / agent status" to avoid implying live data). Final: interactive demo galaxy now in the hero/first viewport (desktop right, mobile below headline) with a fade-in reveal; galaxy_still poster in the old .contrast slot keeps the single interactive galaxy rule. Divergence from the described V1-splash plan is intentional & reviewer-driven; flagged to user with an offer to restore a short non-blocking cinematic fade if they want the more literal 渐入渐出. Gates all pass, leak scan 0.
- [done] 2026-07-07 (claude/team-leader) Promo round 5 — advantage-led landing restructure DONE. 2 competing restructures (A value-prop pillar grid / B pain-narrative + PM console) → team-leader synthesized A-base + B's team-orbit monitoring console → 2 reviewers (PM-lead 7/10, HN-skeptic 7/10) found 4 highs (privacy overclaim/contradiction, console-vs-demo view gap, agent-centric hero) → fix pass resolved all 4. Final page lists all 7 verified advantages (Why-teams-pick-it grid) + dual-audience "One map, two jobs" with a team-orbit console for PMs/leads + "All you need is a GitHub account" band; round-4 honesty/typography gates all re-pass; leak scan 0. Team-leader final gate re-check passed. See round5-synthesizer + round5-fixer entries below. NOT committed — awaiting user approval.
- [done] 2026-07-07 (claude/team-leader) Team-work feature landed in BOTH repos (private commit b5cacc9). Public side: collect.py resolve_user()/load_team()/inject_users() (user as node attribute only — never in ids; sanitize_public strips user data), contribute.sh optional 4th arg, build-public-demo.py 3 fictional users (ada/kael/mira, 445 nodes/1893 edges, cross-user shared project), viewer USER dropdown + machine cascade (refillMachines) + user coloring (team.json member colors or FNV hash) + team-member glyph/legend/readout + bilingual i18n + mobile drawer, README(+zh)/ONBOARDING Team Work sections, team.json.example. Team-leader integration fix: renamed the per-source push-identity key to `amg_user` in collect.py:761,785 + sources.json examples — the spec's "user" key collided with the existing ssh-login key (private side caught it; public side had the bug). E2E: validate_graph pass, demo==viewer byte-identical, galaxy shell 2-line brand diff, landing stats resynced (442→445), zero console errors, leak scan 0.
- [done] 2026-07-07 (claude/round4-implementer) Promo round 4 implementation landed in scripts/build-landing-concepts.py and regenerated docs/ — see the 2026-07-07 round4-implementer entry below. NOT committed.
- [done] 2026-07-07 (claude/team-leader) Promo round 4 — copy & typography overhaul complete. Process: 3 researchers (influential repos / launch pages / tech-blog narratives, web-verified where possible) + 3 fresh-user reviewers (ML researcher 6/10, HN engineer 6.5/10, mobile+zh 6.5/10) ran in parallel; researchers drafted 3 proposals + cross-critiques; team leader synthesized (B-skeleton + A-copy + C-order); 1 implementer + 2 re-review rounds. Final persona scores 9 / 8.5 / 8.5 with zero unresolved highs. Team-leader final gate re-check passed; also scrubbed the leak-scan regex literal (private tokens) that the implementer had quoted into this public log. Remaining known low: demo viewer top-bar wrapping at ~1280px (viewer file, out of round scope). NOT committed — awaiting user approval.
- [done] 2026-07-06 (claude/mobile-pub-viewer) Track (b) 公开侧：viewer/index.html 移动端适配（≤768px 抽屉侧栏、底部详情面板、触摸旋转/捏合、顶栏折叠、DPR≤2），已同步 docs/demo + docs/galaxy（保留 2 行 shell 品牌）。未触碰 build-landing-concepts.py / docs/index.html / docs/assets/*。详见下方 2026-07-06 mobile-pub-viewer 条目。
- [done] 2026-07-06 (claude/team-leader) Promo round 3: (a) researcher-focused landing redesign — two competing variants built in isolated copies; the contrast-launch variant (R2, "Which agent did that?") won over the research-memo variant (R1) and was applied; (b) mobile adaptation of BOTH viewers landed (public via mobile-pub-viewer entry below; private committed as agent-memory-galaxy-private 7496722). See the 2026-07-06 round-3 wrap-up entry below.
- [done] 2026-07-06 (claude/team-leader) Promo polish round 2: (1) landing hero mini-galaxy rebuilt as a cinematic canvas 2D galaxy; (2) demo-vs-private style mismatch diagnosed (root cause: mostly synthetic demo data shape, secondarily over-bright public-only edge lines + hardcoded lang=en) and fixed. See the three 2026-07-06 entries below (impl-subagent, landing-hero-integrator, team-leader wrap-up). NOT committed.

## 2026-07-07 - Round-5 synthesis: A base + B's PM monitoring console (claude/round5-synthesizer)
- What changed: Applied bid A cleanly (`git apply` — value-prop restructure: hero + "Why teams pick it" 6-pillar advantage grid + "One map, two jobs" dual-audience split + "All you need is a GitHub account" band; replaces the old pain-point section), then transplanted ONLY bid B's `team-orbit · live view` monitoring console into A's PM/team-lead audience column (`.audience.pm`). Ported from B's patch: the console CSS subset (`.team-console` + all `.tc-*` rules + `@keyframes tcPulse` + reduced-motion guard + the tc mobile media query), the `team_console_markup()` generator function (4 illustrative rows Ada/Kael/Mira, member colours #7dd3fc/#f0abfc/#fde68a matching docs/demo team.json), and the tc* i18n keys (en+zh paired). De-duped the PM story: A originally repeated PM value in the pillar grid (p6 "A live map for team leads") AND the PM audience bullets m1/m2/m3 — dropped m1/m2/m3 + the redundant `.aud-legend` (the console carries its own legend), replaced with one framing line (new key `audPmLead`) so PM info now reads once as a claim (pillar p6) + once as a picture (console). Removed the now-dead i18n keys m1/m2/m3/audLegWorking/audLegRef/audLegUser (both langs). Added `.aud-lead` to the zh keep-all rule to kill a "跑。" orphan / a split "机器" in the zh lead. Honesty: console keeps the explicit disclaimer "Illustrative demo — a sample team, not live data / 示意 demo——示例团队，非实时数据。" so the red "working" pulses are never presented as real live data.
- Why: Team-leader round-5 synthesis decision — A is the stronger value-prop skeleton, but B's static HTML/CSS console gives "a lead monitors the whole team at a glance" a concrete picture that A's text-only PM column lacked. Generator-only, single source of truth (scripts/build-landing-concepts.py).
- Status: done (parent [doing] round-5 still open for team-leader). NOT committed.
- Verification: `python3 scripts/build-landing-concepts.py` rebuilds cleanly; node --check landing.js OK. Acceptance lines all pass on the rebuilt docs/index.html — galaxy_card==1 (console is HTML/CSS, single canvas intact), team-console==1, STEP 01-05 & 步骤 01-05 continuous, en copy no " - " (only JS arithmetic in the canvas renderer matches), zh 「合成」 0, MIT 0 / no LICENSE, synthetic 0 + fictional 3 (≤3), leak grep the private-identifier denylist 0, zero JS console output on landing en+zh. Headless Chrome (force-scale 1, hidden scrollbars) full-page 1920 en+zh, 1366 en, 390 en+zh + console closeups en+zh (1920 & 390) + concepts gallery 1920, all Read-checked: console reads correctly and fits the half-width PM column at 1920/1366 and full-width at 390 with task text ellipsised (no horizontal overflow — right-edge ink scan on both 390 shots = 2 rows, i.e. section dividers only); zh kicker not uppercased; no orphan/word-split after the keep-all fix; concepts gallery unchanged (5 concept cards + galaxy). Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/round5/synth/. Final section order: hero → Why teams pick it (pillars) → contrast (before/after galaxy) → One map, two jobs (researchers | PM+console) → GitHub-account band → How (STEP 01-05 timeline) → Install → Search/evidence + stat band → Privacy → CTA → footer.
- Files: scripts/build-landing-concepts.py (generator); regenerated docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html.

## 2026-07-07 - Round-5 high-issue fixes: privacy honesty + team-first hero + console/demo view gap (claude/round5-fixer)
- What changed (generator-only, scripts/build-landing-concepts.py; both en+zh i18n dict + HTML fallbacks kept in sync): fixed the 4 HIGH reviewer issues on the round-5 synthesis page (A base + B console).
  (1) Console-vs-demo trust gap [B]: added a bridge line under the team-orbit console (new key `tcBridge`, rendered as `.tc-bridge` inside `.audience.pm` after the console) — "The live product renders this same team data as an interactive galaxy — the demo opens that map, not a list like this." The console's existing `tcDemo` disclaimer only disclaimed the *data*; this sets expectations about the *view* so "Open the demo" landing on the 3D galaxy (no roster) isn't a bait-and-switch. (Chose option (c): copy fix, since building a real roster in the viewer is out of scope / would touch viewer files.)
  (2+4) Privacy overclaim/contradiction [both]: Pillar-2 headline `p2Title` "Plaintext memory never leaves your machine" (flatly false in team mode) → "Plaintext stays local or in your private hub"; `p2How` rewritten to "Plaintext lives on your machines, or in a private repo only your collaborators can read; only a public Pages deploy is encrypted — client-side AES-256-GCM …". Privacy section: `privacyBody` made conditional on solo ("Working solo, your agent_memory.md and fragments never leave your machine — nothing phones home. GitHub Pages carries only the open framework and this demo.") + new `privacyTeam` line ("On a team, distilled fragments — safe session metadata, never raw conversations — sync as plaintext inside your private GitHub repo, readable only by the collaborators you add. The only thing that ever leaves that repo is a public Pages deploy, and that ships AES-256-GCM ciphertext …"). The absolute "never leaves your machine" now appears nowhere; the false-line the HN reviewer would quote is gone.
  (3) Agent-centric hero [both]: kicker `heroKicker` "FOR RESEARCHERS RUNNING MANY AGENTS" → "FOR RESEARCHERS AND TEAMS RUNNING MANY AGENTS"; first supporting line `heroDef` now leads team-first: "See what every teammate's agents shipped today: a Claude Code skill that merges every machine's agent_memory.md into one private, searchable map — a single static HTML page." Headline "Which agent did that?" kept per reviewer.
  (bonus) zh mobile typography: my new privacyBody/privacyTeam introduced single-glyph orphans (e.g. "示。") + mid-word CJK splits at 390; extended the zh `word-break: keep-all; line-break: strict` guard to landing body classes (.hero-scene, .section-head p, .pillar-how, .aud-list li, .privacy-team, .gh-body, .tl-item p, .stat-honest, .contrast-caption). Verified no CJK run exceeds the 390px container (longest ~16 glyphs in tl2Body), so no overflow; this also cleaned up pre-existing baseline orphans (audBody "题。", r1 "案。"). Added `.tc-bridge`/`.privacy-team` CSS. tc-bridge deliberately left OUT of keep-all (its 23-glyph run could overflow the narrow PM column; it already breaks cleanly at the em dash).
- Why: two reviewers (PM + HN engineer) flagged these as must-fix HIGH: the flagship PM console sold a roster board the demo doesn't render; the "never leaves your machine" privacy claim is false for the team mode the page sells (team = plaintext fragments pushed to a shared private repo); and the hero read as a solo-IC tool at the 10-second mark.
- Status: done. NOT committed (per task).
- Verification: `python3 scripts/build-landing-concepts.py` rebuilds clean; node --check landing.js OK. Grep gates on regenerated docs all pass — galaxy-card==1, team-console==1, STEP 01-05 & 步骤 01-05 continuous, MIT 0 / no LICENSE, 合成 0, synthetic 0, fictional 3 (≤3), en index no " - ", leak grep the private-identifier denylist 0, new keys privacyTeam/tcBridge paired en+zh, absolute "never leaves your machine"/「明文记忆永不离开」 0. Headless Chrome via CDP (force-scale 1, hidden scrollbars): full-page 1920/1366/390 en+zh + region closeups (hero, pillar-2, audience+console+bridge, privacy, how) all Read-checked — no horizontal overflow (scrollWidth==innerWidth at every width), zero JS console errors (only a benign /favicon.ico 404 the faviconless page always triggers), zh orphans/mid-word breaks resolved, concepts gallery intact (5 cards + canvas galaxy). Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/round5/fix/.
- Files: scripts/build-landing-concepts.py (generator); regenerated docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html.

## 2026-07-07 - Team-work feature: public-side E2E acceptance + docs regeneration (claude/e2e-pub-acceptance)
- What changed: Regenerated the public demo from the team-work implementation (`python3 scripts/build-public-demo.py`, validate_graph passed) — graph is now 445 nodes / 1893 edges (was 442/1888: +3 `user` nodes ada/kael/mira, +5 `owns` edges), meta gained `users` + `team` (demo-orbit, default_user=unassigned, member colors). Re-synced `docs/galaxy/index.html` from the new viewer and replayed the 2 Encrypted Viewer Shell brand lines (diff vs viewer is exactly those 2 lines). Re-ran `scripts/build-landing-concepts.py` so landing/concepts stats follow the new graph (only 442→445 / 1888→1893 changed; landing.js/css byte-identical, round-4 copy intact).
- Why: E2E acceptance of the multi-user team-work feature on the public side; landing stat band claims its numbers come straight from graph.json, so it must be rebuilt whenever the demo graph changes.
- Verification: validate_graph pass; demo==viewer byte-identical; galaxy diff exactly 2 brand lines; round-4 gates re-pass (galaxy-card==1, STEP/步骤 01-05, 合成/MIT 0); DOM assertions via headless Chrome — fUser options [ALL,ada,kael,mira], machine dropdown refills per user without duplication (mira→{ci-runner-d,pages-host-e}, ada→{workstation-a,laptop-b}, ALL restores 5), invalid machine cleared on user switch, focus convergence 307→161 visible with 0 other-user entries, user color mode returns team.json member colors (#7dd3fc for ada) and grey for userless nodes, user-node readout shows USER row + owns neighbors, no-user graph hides fUser/user-color and falls back to type coloring; zero console errors across 15+ headless shots (1920 en/zh default/filter/color/detail, 390 drawer with USER dropdown, landing); leak scan over full git diff + new files: 0 hits (denylist incl. real names/hosts/IP regex/secrets). Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/teamwork/e2e-pub/shots/. NOT committed (aggregator commits centrally).
- Files: docs/demo/graph.json, docs/demo/index.html, docs/galaxy/index.html, docs/index.html, docs/concepts/*.html (all regenerated/synced; source files untouched by this pass)

## 2026-07-07 - Promo round 4 fix: zh install heading overflow at 390px (claude/round4-fixer)
- What changed: zh install-section h2 copy shortened from 「在 Claude Code 里敲两条命令即可安装。」 to 「敲两条命令，装进 Claude Code。」 in `scripts/build-landing-concepts.py` (the zh i18n dict, formerly line 1381), then regenerated docs.
- Why: Round-4 re-review (U3) found the old zh heading clipped at 390px width: `text-wrap: balance` + zh `word-break: keep-all; line-break: strict` made the second line 「里敲两条命令即可安装。」 an unbreakable 11-glyph run (~418px at 38px font) that overflowed the viewport — the last glyph was half-cut and the full stop invisible. The new copy breaks after 「，」 into two lines that both fit (measured ink right edges 316px / 284px at 390 viewport).
- Verification: CDP per-line ink measurement of all h1/h2 at 390 en+zh (no line exceeds viewport; only intentionally scrollable `.code` elements extend past); PIL right-edge scan of the zh-390 full-page shot (columns 379-389, 0 ink hits over 6600px); zero console errors at 390/1920 en+zh; all BRIEF gates re-pass (galaxy-card ==1, STEP/步骤 01-05 continuous, en " - " 0, 「合成」/MIT 0, leak scan 0); concepts gallery regression OK (concepts only share landing.js renderer code, not the changed copy). Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/round4/fix/. NOT committed.
- Files: scripts/build-landing-concepts.py, docs/assets/landing.js (regenerated; docs/index.html and docs/concepts/* re-emitted unchanged in content)

## 2026-07-07 - Promo round 4: copy & typography overhaul of the landing (claude/round4-implementer)
- What changed: Implemented the team leader's finalized plan (B-skeleton + A-copy + C-order) in `scripts/build-landing-concepts.py` only, then regenerated docs. (1) Hero: kept the "Which agent did that?" hook, added a <=25-word definition sentence naming skill/agent_memory.md/static-HTML under the H1, compressed the 55-62-word scene paragraph to two short sentences, replaced the grey pills with a gold mono evidence row "0 daemons · 1 static HTML file · 2 commands" + "Private by default — publishing ships ciphertext only.", main CTA now "Open the demo"/「打开在线演示」with microcopy "Demo uses fictional data."/「演示为虚构数据。」 (2) IA cut ~10 sections to 7: Hero(with tightened before/after: 5 chips + 2 enlarged question terminals, wider WITH panel) → pain (PAIN 03 now "No handoff between agents") → how (STEP 01-05 continuous, STEP 04 = LIVE) → install (moved up, "Install with two Claude Code commands.", right card "Try the demo locally") → demo section (second galaxy card deleted — galaxy-card appears exactly once; replaced by a pure-HTML evidence readout card styled after the viewer detail panel with who/when/machine/files/why/status fields using demo-graph-consistent fictional content + "This is what evidence looks like."; stat band folded in here) → privacy (direct promise "never leave your machines … Nothing phones home." + README link; URL table and Contributor/Aggregator cards deleted) → CTA. Nav is now Demo · Install · Privacy · GitHub with Design archive moved to the footer. (3) Honesty: stat title "The synthetic demo, by the numbers"/「演示数据规模一览」, "agents live right now"+red pulse → "simulated agents active" (pulse removed), synthetic/fictional statements converged to 3 spots (hero microcopy, stat footnote, footer), no MIT text (repo has no LICENSE), no untested time promises. (4) Typography/zh: en " - " → em dash everywhere, curly quotes in CTA/pain copy, h1/h2 text-wrap:balance, zh word-break:keep-all + line-break:strict on headings + hero definition (kills 改/的?、记/忆、页/面 orphans), zh kicker de-uppercased and reworded, zh rewrite: 「合成」→「虚构/演示数据」(0 hits left), 「读出面板」→「详情面板」, 「其它」→「其他」, agents→agent, full-width punctuation. (5) Mobile: 390px page now EN 7.86 / zh 7.32 screens (was ~8.8+), code blocks keep horizontal scroll with a right-edge fade hint (background-attachment:local trick), Install link stays in the mobile nav, pain figs hidden <=600px, action buttons 2-col grid on small screens.
- Why: Round-4 review found the hero never said what the product is, STEP numbering skipped 04, "REAL NUMBERS" contradicted the fictional footnote, the galaxy card appeared twice, zh copy read as translated English with orphaned glyph line breaks, and install was buried at 65% page depth.
- Status: done. NOT committed (per task).
- Verification: grep gates all pass (galaxy-card ==1 in docs/index.html; STEP 01-05 and 步骤 01-05 continuous; " - " 0 in en copy; 「合成」/「其它」/「读出」 0; MIT 0; leak scan against the private-identifier denylist → 0 hits); node --check on generated landing.js; headless Chrome full-page shots at 1920/1280/390 en+zh reviewed (no orphan CJK breaks, no overflow; hero spec row wraps on 390); zero JS console errors on landing en/zh and concepts pages; concepts gallery + bento-proof regression OK (shared galaxy_card/nav changes text-only). Screenshots: /tmp/claude-1000/-home-cyber-Renyun-Project/44cdf6b6-48c6-4dec-a604-f7fce41b0c52/scratchpad/round4/impl/. Pending human check: modal click-through and copy buttons on a real browser.
- Files: scripts/build-landing-concepts.py, docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html (regenerated)

## 2026-07-06 - Promo round 3: researcher pain-point landing + wrap-up (claude/team-leader)
- What changed: Landing rebuilt around the researcher pain narrative (winning variant R2 of two competing redesigns): hero asks "Which agent did that? / 昨天是哪个 agent 改的？" with a before/after block (chaotic unconnected agent collage, all fictional demo-* names, vs the canvas galaxy with gold collaboration edges), three pain cards (nobody remembers who did what / duplicated work / no collaboration network), honest stats band injected from graph.json meta (all labeled synthetic/fictional), timeline how-it-works, second full-width galaxy instance, URL-sharing policy table, closing CTA "Stop asking which agent did that". ~41 new i18n keys per language; canvas galaxy renderer untouched (new call sites only). The losing R1 variant (light-paper technical-memo layout with a work-ledger listing and BibTeX citation block) is preserved as a patch in the session scratchpad for a possible future concepts page.
- Why: The audience is researchers running many agents daily; the pain (forgetting which agent did what, no collaboration network between agents) had to be the page's first visual, and the dark cosmic identity had to stay continuous with the demo viewer.
- Status: done
- Verification: fold/full screenshots at 1920/1366/768/390 en+zh, concepts regression, reduced-motion fallback, zero console errors, leak scan over full diff (0 hits), R2 patch applied cleanly on HEAD 8fc12f9 and landing regenerated. Judged by team leader against R1 on impact, brand continuity with the demo, pain-point directness, and mobile cleanliness.
- Files: scripts/build-landing-concepts.py, docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html
- Cross-repo note: the private viewer got the same mobile spec (drawer sidebar, touch rotate/pinch, bottom detail sheet, dvh, DPR<=2) in agent-memory-galaxy-private commit 7496722. Known low-priority follow-up: public and private mobile implementations use slightly different media queries and element ids (public: max-width:768px OR max-height:480px, #sideToggle; private: max-width:768px OR pointer:coarse<=1024px, #panelToggle) — spec-identical behavior, worth unifying names next sync. Pending human check: real-device touch feel (pinch inertia, iOS safe-area) and 30s+ fps on a DPR=2 laptop.

## 2026-07-03 - Public framework scaffold (codex)
- What changed: Prepared a public-safe framework for multi-machine agent memory graphs, including a marketing page, runtime viewer separation, synthetic demo generation, and skill/plugin packaging.
- Why: Public repositories should explain the system and provide install paths without exposing real memory fragments, machines, projects, or private graph data.
- Status: done
- Verification: Run the public demo builder and privacy leak scan before release.
- Files: README.md, AGENTS.md, ONBOARDING.md, PORTABILITY.md, docs/index.html, viewer/index.html, scripts/build-public-demo.py, plugins/agent-memory-galaxy/skills/agent-memory-galaxy/SKILL.md

## 2026-07-06 - Public demo visual alignment and bilingual launch polish (codex)
- What changed: Synchronized the public demo viewer closer to the private viewer visual baseline by restoring the default `cosmos` style, making demo URLs explicit with `style=cosmos`, adding public demo edge labels, and extending galaxy glyph mapping for `boundary` and `artifact` nodes. Reworked the promotional hero thumbnail from a static mini layout into a public-safe animated SVG micro-galaxy with glowing nodes, flowing edges, scan motion, responsive panels, and reduced-motion support. Added bilingual README/demo URL updates and kept the promo/demo language switching intact.
- Why: The public repository is intended for promotion and skill adoption, so the demo needs to look consistent with the private viewer while remaining synthetic, attractive, bilingual, and safe to publish.
- Status: done
- Verification: Regenerated `docs/demo` and landing assets, checked Python syntax, JS syntax, JSON validity, `git diff --check`, local HTTP routes, Chrome headless layout and canvas rendering, private artifact tracking, secret/path leak scan, and live GitHub Pages URLs. Pushed commit `5972c50` and confirmed online pages returned 200 with latest content.
- Files: README.md, README.zh-CN.md, viewer/index.html, docs/demo/index.html, docs/galaxy/index.html, scripts/build-landing-concepts.py, docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html

## 2026-07-06 - Cosmos style alignment: demo data shape + viewer polish (claude/impl-subagent)
- What changed: Reshaped `build-public-demo.py` synthetic data to match the private baseline's visual form — long-tail weights (entry/fact/file 1-40, method/tech 5-80, dataset/model 50-400, machine/server 300-1500, project 2100-6000; graph now 442 nodes / 1888 edges vs 137/466), entry dates now relative to GENERATED_AT covering ~45 days with the last 3 days holding 4 entries each (restores the green breathing layer), and 6 extra `references` edges weaving a gold skeleton between the two heaviest project hubs. Viewer: dimmed public-only publish/boundary edge class (alpha 0.48→0.2, lw 1.8→1.0, dot r 2→1.4), `readLang()` now falls back to `navigator.language`, `TOOLC` gained `automator`, zh stat keys restored to English (`ONLINE · N NODES …` baseline style), style trigger label follows language (zh/en half of theme label). `update.sh` now re-applies the 2-line Encrypted Viewer Shell branding after its bare `cp` so docs/galaxy keeps its title/h1.
- Why: Public demo looked flat vs the private baseline (uniform node sizes, stale dates, sparse gold skeleton, over-bright publish lines); promotion needs the same dust-to-hub galaxy hierarchy while staying fully synthetic.
- Status: done (this subtask; parent [doing] still covers the landing-hero track)
- Verification: build + validate_graph pass, node JS syntax check, headless Chrome screenshots at 1920/1366 en+zh + galaxy style + shell with zero console errors, leak scan over git diff (0 hits), docs/galaxy diff vs viewer is exactly the 2 brand lines.
- Files: scripts/build-public-demo.py, viewer/index.html, docs/demo/index.html, docs/demo/graph.json, docs/galaxy/index.html, update.sh

## 2026-07-06 - Landing hero: cinematic canvas 2D mini-galaxy (claude/landing-hero-integrator)
- What changed: Replaced the hero card's animated SVG micro-galaxy with a cinematic canvas 2D renderer (winning variant A): 3-layer parallax starfields, twin log-spiral disc sprite (0.62 squash, ~209s rotation), 3 drifting nebula sprites, 48 typed nodes pinned to the arms (project/server/agent/boundary/artifact, 3 live agents with pulse rings), 14 curved edges with golden comet-tail flows, meteors, camera drift + breathing zoom + vignette. All sprites pre-rendered offscreen; rAF loop pauses on document.hidden / out-of-view. Static SVG remains as noscript/reduced-motion fallback. Review fixups applied: (1) runtime prefers-reduced-motion change listener now stops the loop, releases sprites, and restores the SVG (and resumes on switch-back); (2) first frame is drawn synchronously before the SVG is hidden, killing the load-time flash; (3) nebula anchors moved to left-middle/bottom-right and camera drift amplitude raised (0.013→0.021 W, 0.015→0.02 H) to balance the empty corners at 1920.
- Why: Promo round 2 - the hero preview needed to look like a real galaxy (closer to the private viewer's cosmos feel) while staying synthetic, accessible, and cheap to render.
- Status: done
- Verification: node --check on landing.js, python ast parse, headless Chrome shots at 1920/1366/768/390(+long)/zh/reduced-motion plus concepts gallery (1920+1100) and all 5 concept subpages - no JS errors, no clipping, container queries hide HUD/legend on narrow cards, reduced-motion shows the static SVG; a matchMedia-stub harness confirmed the runtime reduced-motion toggle stops/resumes the loop; 300ms early-frame shot confirmed no blank-canvas flash; leak scan over the diff: 0 hits. NOT committed (per task). Pending human check: real-browser 30s+ fps observation on a DPR=2 laptop incl. background-tab suspend/resume.
- Files: scripts/build-landing-concepts.py, docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html

## 2026-07-06 - Promo round 2 wrap-up: final integration (claude/team-leader)
- What changed: Coordinated the two tracks above via a 10-agent workflow (hero A/B design + judge; private/public spec extraction + diagnosis + alignment + independent verification). Final integration: removed the 5 hardcoded `?style=cosmos&lang=en` demo links from `build-landing-concepts.py` (landing.js demoUrl() already overrides lang at runtime; removal lets the viewer's navigator.language fallback serve no-JS visitors), regenerated landing (stats auto-synced to the new 442 nodes / 1888 links graph), and re-verified with headless Chrome at 1920/390 + demo zh.
- Why: User feedback - the hero thumbnail was not attractive enough, and the demo opened from the promo page did not match the private viewer's look. Diagnosis verdict: ~80% synthetic demo data shape (too few nodes, uniform weights, stale dates, sparse gold skeleton) / ~20% viewer (over-bright public-only publish edges) + landing forcing lang=en; the rendering code itself is line-for-line shared with the private baseline.
- Status: done. NOT committed/pushed (user did not request it).
- Verification: independent verifier scored alignment 8-9.5/10 per dimension with only low-severity residuals; zero JS console errors on all screenshots; leak scan over full git diff (private hostnames/IPs/project names): 0 hits; final landing/demo screenshots checked at 1920/1366/768/390, en+zh, reduced-motion. Pending human check: real-browser 30s+ fps observation on a DPR=2 laptop (headless cannot measure frame rate).
- Files: scripts/build-landing-concepts.py, docs/index.html, docs/concepts/*.html (final lang-link cleanup); everything else per the two entries above.

## 2026-07-06 - Public viewer mobile adaptation (claude/mobile-pub-viewer)
- What changed: `viewer/index.html` gained a shared-spec mobile mode, active at `@media (max-width:768px),(max-height:480px)` (short-viewport clause catches landscape phones headlessly; `pointer:coarse` additionally bumps touch targets on real touch devices). Details: (1) meta viewport now has `viewport-fit=cover`; (2) left control panel becomes an off-canvas drawer (`#side.open`, width `min(80vw,320px)`, full height, slide transition) with a 52px floating `#sideToggle` bottom-left carrying a visible-node-count badge (`#stBadge`, updated in `applyFilter`) and a click-to-close `#sideBackdrop`; `#panel=open` URL hash deep-opens the drawer for headless/QA; (3) top bar wraps to two rows — h1 ellipsized, full stats+live wrapped in `#statwrap` (desktop `display:contents`, mobile hidden) behind a compact `#statBadge` popover toggle, search collapsed behind a `#searchBtn` icon that expands the input to a full-width 16px row; (4) canvas touch: single-finger drag rotates, two-finger pinch zooms, tap selects (touchstart/move/end with preventDefault + `touch-action:none`, mouse/wheel untouched); (5) `#detail` becomes a bottom sheet (full width, max-height 55vh, internal scroll, 44px close, slide-up animation); (6) DPR clamped to 2 in `resize()`, cosmos star density divisor 2300→3400 on mobile, `#hint` hidden on mobile, side-panel fonts ≥11px and controls ≥44px; (7) new UI keys `panelBtn`/`statsBtn` for button titles in both languages. Synced products: `scripts/build-public-demo.py` re-ran (graph.json byte-identical, 442 nodes/1888 edges), `docs/galaxy/index.html` re-copied with the 2 brand lines (title + h1) restored — diff vs viewer is exactly those 2 lines, matching what `update.sh` replays.
- Why: user feedback that the public demo is painful on phones; promo round 3 track (b) requires both viewers to share one responsive spec (same breakpoint/class names/interaction modes) so private can mirror it.
- Status: done. NOT committed (per task).
- Verification: node --check on extracted inline JS; headless Chrome shots at 390x844 (zh+en), 360x800, 414x896, 844x390 landscape, 768x1024 (zh+en), plus drawer-open (`#panel=open` zh+en) and injected-state shots for detail sheet / stat popover / expanded search — all reviewed; 1920x1080 zh+en confirm desktop pixel-wise unchanged behavior; chrome --enable-logging=stderr: zero JS console errors; leak scan over git diff: 0 hits; screenshots in /tmp/amg-mobile-shared/mobile/pub/. Pending human check: real-device touch feel (pinch inertia, iOS safe-area) — headless cannot exercise real gestures.
- Files: viewer/index.html, docs/demo/index.html, docs/galaxy/index.html (both synced copies), agent_memory.md

## 2026-07-07 - Round-6 synthesis: cinematic splash + demo-first hero (claude/round6-integrator)
- What changed: Synthesized two round-6 candidates into scripts/build-landing-concepts.py. Base = V1 (applied clean on top of ac1615a; V1 already carried round-5): (a) cinematic splash overlay on open — deep-space veil + gold "AGENT MEMORY GALAXY" kicker + bilingual tagline "One galaxy. Every agent." / "一张银河，容纳每个 agent。" that dissolves to reveal the first screen; parser-blocking head script gates it via sessionStorage amg_intro_seen (plays at most once/session) + Skip button + prefers-reduced-motion static + ?intro=skip/play; (b) hero became hero-split with the single interactive galaxy_card (element_id="hero-galaxy") in the first fold; the before/after "after" side switched to a non-interactive galaxy_still poster (no canvas/expand/role=button) so the interactive galaxy stays unique. Ported from V2: the fuller hero card is realized via the shared galaxy_card frame (framebar "demo graph preview" + "445 nodes / 1893 links / 5 machines" stats from graph.json, NODE TYPES legend, ONLINE HUD "multi-user hub / live agents / encrypted boundary") PLUS a new caption below the card: stageHint en "Interactive demo — drag, zoom, click a node." / zh "可交互 demo——拖拽旋转、滚轮缩放、点节点看详情。" (added as a paired i18n key + .hero-stage-hint CSS with zh keep-all). Also widened the hero-split visual column (minmax(0,1fr) minmax(430px,.96fr) → minmax(0,.88fr) minmax(500px,1.02fr)) so the desktop card clears the 560px @container threshold that hides the framebar stats + ONLINE HUD — without it they collapsed in the fold.
- Why: Round-6 asked for (1) a galaxy "fade-in/fade-out" opening animation on page open and (2) moving the demo into the first screen (no scroll), while keeping the fuller V2 card framing.
- Status: done. NOT committed/pushed (per task).
- Verification: python ast parse OK; rebuilt; on docs/index.html — exactly 1 galaxy-card / 1 <canvas> / 1 data-mini-canvas, galaxy-still has no canvas/expand/role=button; all 141 data-i18n keys present in both en+zh; leak scan the private-identifier denylist 0 hits; no " - " in i18n values or index prose; no 「合成」; no MIT; STEP 01-05 present; text-wrap:balance on h1/h2; CTA curly quotes en/zh. Headless Chrome (virtual-time 200/900/1800/3500 en+zh intro; folds 1920/1366/390 en+zh; reduced-motion en+zh; CDP full-page en+zh; CDP sessionreload proving intro plays on load #1 and NOT on load #2; concepts gallery) all reviewed: fold shows stats+HUD+caption at 1920/1366, mobile 390 collapses HUD/stats by container query (clean, not broken) with caption below, reduced-motion shows the static SVG galaxy with no overlay. Only console noise is the environmental /favicon.ico 404 (no JS exceptions/console.error). Shots in scratchpad round6/synth/.
- Files: scripts/build-landing-concepts.py, docs/index.html, docs/assets/landing.css, docs/assets/landing.js, docs/concepts/*.html
